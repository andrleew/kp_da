#include <iostream>
#include <fstream>

#include <unistd.h>

using namespace std;
int main(){
    cin.tie(0);
    ios::sync_with_stdio(false);
    for (int i = 1; i > 0; ++i){
        for (int j = 1; j <= i; ++j){
            
        }
    }
    return 0;
}